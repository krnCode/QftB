version: 1

models:
  - name: stg_rawg__tags
    description: Staging table for the rawg_tags table
    columns:
      - name: tag_id
        description: "Primary key for the tag"
        tests:
          - unique
          - not_null
      - name: tag_name
        description: "Name of the tag"
        tests:
          - not_null